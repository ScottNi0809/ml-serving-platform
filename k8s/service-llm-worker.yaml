# service-llm-worker.yaml
apiVersion: v1
kind: Service
metadata:
  name: llm-worker
  labels:
    app: llm-worker
spec:
  type: ClusterIP
  selector:
    app: llm-worker
  ports:
    - port: 8003
      targetPort: 8003
      protocol: TCP
      name: http