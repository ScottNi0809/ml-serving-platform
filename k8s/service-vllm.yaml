# service-vllm.yaml
apiVersion: v1
kind: Service
metadata:
  name: vllm
  labels:
    app: vllm
spec:
  type: ClusterIP
  selector:
    app: vllm
  ports:
    - port: 8100
      targetPort: 8100
      protocol: TCP
      name: http